{
  "name": "@mimic-fi/v3-deployments-core",
  "version": "0.0.1",
  "license": "GPL-3.0",
  "files": [
    "deploys/**/*"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "eslint . --ext .ts",
    "test": "hardhat test",
    "test:mainnet": "hardhat test --fork mainnet --chain-id 1 --block-number 17735210",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@mimic-fi/v3-helpers": "0.0.7"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "2.0.3",
    "@types/node": "^14.6.0",
    "@types/node-fetch": "^2.5.12",
    "eslint-config-mimic": "^0.0.2",
    "ethereum-waffle": "^3.4.4",
    "ethers": "~5.6.0",
    "hardhat": "^2.14.1",
    "hardhat-local-networks-config-plugin": "^0.0.6",
    "ts-node": "^10.9.1",
    "typescript": "~4.3.4"
  },
  "eslintConfig": {
    "extends": "eslint-config-mimic"
  }
}
